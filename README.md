Simple test compine of VueJS, NodeJS and mongoDB

to install:
clone repository to new-folder
cd new-folder
npm install
npm run dev

to launch node:
type in terminal from root of directory - nodemon server
